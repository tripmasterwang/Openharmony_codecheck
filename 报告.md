功能要求：

一、修改工具逻辑，直接在原代码目录中修改：类似Claude Code, Gemini CLI（已修改）

已修改逻辑，直接在原代码目录中修改，思路是复制一份源文件到临时目录~/tmp/harmocheck。之前的代码都可以复用，之前是源文件保持不变，现在是要让~/tmp/harmocheck保持不变，之前的代码是修改 -o参数下路径的代码，现在要取消-o参数，直接修改源文件。


二、缺陷列表通过参数传入，不要在项目里面， 调用命令类似：harmocheck -d <path\of\ISSUE_DESP.js>

答：代码逻辑已修改，并验证以下指令可用：
harmocheck -i ./dataset1/openharmony/test/vendor_telink -d /data2/wangyuansong/project2/harmocheck/dataset1/openharmony/ISSUE_DESP.xlsx -w 5 -m openai/deepseek-v3.2-exp

三、支持官网Deepseek，遵循OpenAI格式的大模型

答：不再使用liteLLM来协调api接口，统一使用openAI兼容api（其余的不支持），已经验证以下命令有效：

harmocheck -i ./dataset1/openharmony/test/vendor_telink \
  -d /data2/wangyuansong/project2/harmocheck/dataset1/openharmony/ISSUE_DESP.xlsx \
  -w 5 \
  -m deepseek-reasoner

用户新增模型的方法，已经更新在readme当中。

四、如何举一反三？因为规则引擎有可能不会返回iu所有缺陷 测试PR：

答：已经尝试：api每解决完一个问题，都会在全项目中寻找类似问题，结果是非常之慢，对于大型项目并不可取，不建议采用。

https://gitcode.com/openharmony/resourceschedule_soc_perf/pull/182

五、harmonycheck_traj这些定位文件建议放在用户目录下的某个隐藏目录，不要输出在修复代码文件夹里面

答：已经全放在~/.local/share中。

材料问题:
评委提问：这个CLI工具与Claude Code/ Gemini CLI/Qwen Code这些CLI工具+缺陷JSON的区别？

1. 在众多脚手架中，为什么选择mini swe agnet作为脚手架。
1.1. 项目级代码修复专业性：swebench leader board（我们的测试指标）中，证明了sweagent脚手架的优越性，至于Claude Code/ Gemini CLI/Qwen Coder，无。
1.2. Claude Code不开源，api绑定，昂贵，不考虑
1.3. gemini cli未测试
1.4. Qwen Coder未测试